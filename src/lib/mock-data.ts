import { Pedido, Movimento } from './types';

// Removendo mocks para usar exclusivamente dados reais do Firestore
export const MOCK_PEDIDOS: Pedido[] = [];
export const MOCK_MOVIMENTOS: Movimento[] = [];
