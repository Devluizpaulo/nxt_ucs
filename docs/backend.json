
{
  "entities": {
    "Pedido": {
      "title": "Pedido",
      "description": "Representa um pedido de crédito (UCS) com detalhes de auditoria e rastreabilidade blockchain.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "data": { "type": "string", "format": "date-time" },
        "empresa": { "type": "string" },
        "cnpj": { "type": "string" },
        "programa": { "type": "string" },
        "uf": { "type": "string" },
        "quantidade": { "type": "number" },
        "valorTotal": { "type": "number" },
        "hashPedido": { "type": "string" },
        "linkNxt": { "type": "string", "format": "uri" },
        "auditado": { "type": "boolean" },
        "status": { "type": "string", "enum": ["pendente", "ok", "erro"] },
        "categoria": { "type": "string", "enum": ["selo", "Saas_Tesouro_Verde", "Saas_BMV"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "data", "empresa", "cnpj", "quantidade", "valorTotal", "status", "categoria", "createdAt"]
    },
    "Movimento": {
      "title": "Movimento",
      "description": "Registro individual de movimentação de ativos associado a um pedido.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "pedidoId": { "type": "string" },
        "raw": { "type": "string" },
        "hashMovimento": { "type": "string" },
        "tipo": { "type": "string" },
        "origem": { "type": "string" },
        "destino": { "type": "string" },
        "quantidade": { "type": "number" },
        "duplicado": { "type": "boolean" },
        "validado": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "pedidoId", "hashMovimento", "origem", "destino", "quantidade", "createdAt"]
    },
    "EntidadeSaldo": {
      "title": "EntidadeSaldo",
      "description": "Representa um produtor ou associação com seus saldos de UCS e histórico de auditoria técnica.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "nome": { "type": "string" },
        "documento": { "type": "string" },
        "originacao": { "type": "number" },
        "movimentacao": { "type": "number" },
        "aposentado": { "type": "number" },
        "bloqueado": { "type": "number" },
        "aquisicao": { "type": "number" },
        "saldoAjustarImei": { "type": "number" },
        "saldoFinalAtual": { "type": "number" },
        "saldoLegadoTotal": { "type": "number" },
        "status": { "type": "string", "enum": ["disponivel", "bloqueado", "inapto"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "nome", "documento", "status", "createdAt"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/pedidos/{pedidoId}": {
      "schema": "#/backend/entities/Pedido",
      "description": "Coleção principal de pedidos de crédito."
    },
    "/pedidos/{pedidoId}/movimentos/{movimentoId}": {
      "schema": "#/backend/entities/Movimento",
      "description": "Subcoleção de movimentos de um pedido."
    },
    "/produtores/{produtorId}": {
      "schema": "#/backend/entities/EntidadeSaldo",
      "description": "Saldos auditados de produtores individuais."
    },
    "/associacoes/{associacaoId}": {
      "schema": "#/backend/entities/EntidadeSaldo",
      "description": "Saldos auditados de associações."
    }
  }
}
